<!DOCTYPE html>
<html>
    <head>
        <%- include ('../partials/head.ejs') %>
    </head>
    <body>
    
        <div class="container">
            <h1 class="mt-5 mb-3"><%=patient.name%>'s Profile</h1>
            <div class="row">
                <div class="col-lg-6">
                    <button class="btn btn-warning btn-lg mb-3 hvr-float-shadow" onclick="location.href='/patients/<%=patient.id%>/edit'">Edit Patient Information</button>

                    <button class="btn btn-danger btn-lg mb-3 d-block hvr-float-shadow" type="button" data-toggle="modal" data-target="#delete-patient-modal">Delete Patient</button>

                    <form id="delete-patient-form" class="d-none" action="/patients/<%=patient.id%>?_method=DELETE" method="POST">
                    </form>
                </div>
                <div class="col-lg-6">
                    <button class="btn btn-dark btn-lg mb-3 d-block hvr-float-shadow" onclick="location.href='/patients/'">Back To Patient List</button>
               
                    <button class="btn btn-info btn-lg mb-3 d-block hvr-float-shadow" onclick="location.href='/patients/<%=patient.id%>/visits/new'">Add New Visit</button>
                </div>

            </div>
            <div class="row">
                <div class="col-lg-6">
                    <div class="card bg-light mb-3">
                        <div class="card-body p-3">
                            <h2 class="h2">DEMOGRAPHIC INFORMATION</h2>
                        </div>
                        <div class="card-body">
                            <p><strong>Date of Birth:</strong> <%=patient.birthday%></p>
                            <p><strong>Gender:</strong> <%=patient.gender%></p>
                            <p><strong>Height:</strong> <%=patient.height%> inches</p>
                            <p><strong>Weight:</strong> <%=patient.weight%> lbs</p>
                        </div>
                        
                    </div>
                    
                    <div class="card bg-light mb-3">
                        <div class="card-body p-3">
                            <h2 class="h2">PAST MEDICAL HISTORY</h2>
                            <p><%=patient.medicalConditions%></p>
                        </div>
                    </div>
                   
                    <div class="card bg-light mb-3">
                        <div class="card-body p-3">
                            <h2 class="h2">ALLERGIES</h2>
                            <p><%=patient.drugAllergies%></p>
                        </div>
                    </div>
                    
                </div>
                
                <div class="col-lg-6">
                    <div class="card bg-light mb-3 p-3">
                        <h2 class="h2">VISIT SUMMARY</h2>
                    <% for (let i = 0; i < patient.visits.length; i ++ ) { %>
                        <div class="card bg-light mb-3">
                            <div class="card-body p-3">
                                <div class="row mb-4">
                                    <div class="col-9">
        
                                        <p><strong>Date of Service:</strong> <%=patient.visits[i].dateOfService%></p>
                                        <p><strong>Current Medication List:</strong> <%=patient.visits[i].rxList%></p>
                                        <p><strong>Doctor's Orders:</strong> <%=patient.visits[i].drsOrders%></p>
                                    </div>
                                    <div class="col-3">
                                        <button class="btn btn-warning mb-3 hvr-float-shadow" onclick="location.href='/patients/<%=patient.id%>/visits/<%= i %>/edit'">Edit</button>
                                    
                                    </div> 
                                </div>
                            </div>
                        </div>   
                    <% } %>
                    </div>
                    
                </div>
            </div>

            <div class="modal fade" id="delete-patient-modal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header">
                        <h5 class="modal-title" id="deleteModalLabel">Are you sure you want to delete this patient?</h5>
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                        </div>
                        <div class="modal-body">
                        You cannot undo this change!
                        </div>
                        <div class="modal-footer">
                        <button type="button" class="btn btn-light btn-lg" data-dismiss="modal">Cancel</button>
                        <button type="button" id="confirm-delete-patient-btn" class="btn btn-danger btn-lg">Confirm</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <script>
            // click handler for confirming to delete patient from the modal
            $('#confirm-delete-patient-btn').on('click', () => {
                $('#delete-patient-form').submit();
            })
        </script>
    </body>
</html>